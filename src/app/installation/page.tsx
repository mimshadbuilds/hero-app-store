// 'use client'

// import React, { useContext } from "react";

// const InstallationPage = () => {
//   const { installedApps } = useContext(AppContext);
//   return <div>{InstalledApps.length > 0 ? "" : ""}</div>;
// };

// export default InstallationPage;
